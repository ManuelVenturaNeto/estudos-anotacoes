[project]
name = "sqlmodel-vs-sqlalchemy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Manuel Ventura", email = "manuel.ventura.neto@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.52",
    "sqlmodel>=0.0.39",
    "uvicorn>=0.52.3",
]

[project.scripts]
sqlmodel-vs-sqlalchemy = "sqlmodel_vs_sqlalchemy:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
