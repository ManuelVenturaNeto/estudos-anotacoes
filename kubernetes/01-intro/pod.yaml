apiVersion: v1
kind: Pod
metadata:
  name: webserver
  labels:
    name: exemplo_didatico
    type: webserver
spec:
  containers:
  - name: apache
    image: httpd
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80

